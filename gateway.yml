spring:
  cloud:
    gateway:
      routes:
      - id: name
        uri: lb://name
        predicates:
        - Path=/name/**
      - id: greeting
        uri: lb://greeting
        predicates:
        - Path=/greeting/**
